---
import { cn } from '@/lib/utils';
import { cva } from 'class-variance-authority';
import { overflowVariants } from '@/lib/component-variants';

const listVariants = cva('flex flex-col max-h-screen', {
    variants: {
        overflow: overflowVariants,
    },
});

export interface Props {
    overflow?: 'auto' | 'fixed' | 'expand' | 'wrap' | 'show';
    class?: string;
    [key: string]: any; // Alpine.js pass-through
}

const {
    overflow = 'auto',
    class: className = '',
    ...alpineProps
} = Astro.props;

const classes = cn(listVariants({ overflow }), className);
---

<div class={classes} {...alpineProps}>
    <slot />
</div>
