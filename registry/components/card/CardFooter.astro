---
import { cn } from '@/lib/utils';

export interface Props {
    className?: string;
    // Alpine.js pass-through
    [key: string]: any;
}

const { className, ...alpineProps } = Astro.props;
---

<div
    class={cn(
        'flex items-center px-6 py-3 pb-4 card-footer gap-2 justify-end',
        className
    )}
    {...alpineProps}
>
    <slot />
</div>
